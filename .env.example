# =========================
# Base Treasury Guard
# Environment Variables
# =========================
# Notes:
# - NEVER commit your real private keys
# - Copy this file to `.env` and fill real values locally:
#     cp .env.example .env
# - For Base mainnet:
#     CHAIN_ID=8453
# - For local Anvil:
#     CHAIN_ID=31337

# ---------
# Network
# ---------

# HTTP JSON RPC endpoint (Alchemy recommended)
# Example Base mainnet:
# RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_KEY
# Example Base sepolia:
# RPC_URL=https://base-sepolia.g.alchemy.com/v2/YOUR_KEY
# Example local Anvil:
# RPC_URL=http://127.0.0.1:8545
RPC_URL=http://127.0.0.1:8545

# WebSocket endpoint for event subscriptions (Alchemy recommended)
# Example Base mainnet:
# WS_URL=wss://base-mainnet.g.alchemy.com/v2/YOUR_KEY
# Example Base sepolia:
# WS_URL=wss://base-sepolia.g.alchemy.com/v2/YOUR_KEY
# Example local Anvil:
# WS_URL=ws://127.0.0.1:8545
WS_URL=ws://127.0.0.1:8545

# Chain ID (Base mainnet=8453, Base sepolia=84532, Anvil=31337)
CHAIN_ID=31337

# Deployed TreasuryGuard contract address on the selected network
# Fill this after deployment.
CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# Human friendly network name for presets in config (optional)
# Suggested: base-mainnet, base-sepolia, local
NETWORK=local

# -------------------------
# Keys (DO NOT COMMIT REAL)
# -------------------------

# Private key used by the watcher to submit approvals (0x + 64 hex chars)
GUARDIAN_KEY=0xYOUR_PRIVATE_KEY

# Private key used by the watcher to execute batches (0x + 64 hex chars)
EXECUTOR_KEY=0xYOUR_PRIVATE_KEY

# -------------------------
# Watcher runtime settings
# -------------------------

MAX_BATCH=10
POLL_INTERVAL=5s

# Gas floor forwarded to executeBatch to reduce under-gassed calls
GAS_FLOOR=50000

# API server and metrics endpoints
HTTP_LISTEN_ADDR=127.0.0.1:9000
LOG_LEVEL=info
METRICS_NAMESPACE=treasury_guard
METRICS_ADDR=127.0.0.1:9001

# -------------------------
# Policy controls
# -------------------------

# 0 means "no max" in current code, set to a value in wei to cap.
POLICY_MAX_AMOUNT=0

# Comma separated allowlist of ERC20 token addresses, blank means allow all
# Example:
# POLICY_ALLOWED_TOKENS=0xToken1,0xToken2
POLICY_ALLOWED_TOKENS=